card = [i for i in range(1, 20+1)]

for _ in range(10):
    a, b = map(int, input().split())

    tmp = card[a-1:b]
    tmp = tmp[::-1]
    card[a-1:b] = tmp

print(*card)

# 역배치(역순) 하는건 내림차순 즉 .sort(reverse=True)가 아니라 [::-1]로 해야함

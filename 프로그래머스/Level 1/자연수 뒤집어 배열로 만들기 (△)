"""
문제 제대로 읽자.. 내림차순 정렬이 아니라 그냥 숫자 '뒤집기'이다.
"""

def solution(n):
    
    tmp = list(str(n))
    tmp = tmp[::-1]
    answer = list(map(int, tmp))
    
    return answer

ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ


def solution(n):

    return list(map(int, reversed(str(n))))

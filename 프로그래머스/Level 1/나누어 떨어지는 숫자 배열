def solution(arr, divisor):
    
    answer = []
    
    for i in arr:
        if i % divisor == 0:
            answer.append(i)
            
    if not answer: # 빈 리스트이면
        answer.append(-1)
    else: # 빈 리스트가 아니면 정렬
        answer.sort()
        
    return answer

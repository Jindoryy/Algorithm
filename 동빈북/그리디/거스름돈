"""
이 문제에서 큰 화폐부터 카운트 해서 답이 나오는 이유는 동전 중에서 큰 단위가 항상 작은 단위의 배수이므로 가능한거임
만약 무작위 화폐가 주어진다면 그리디 알고리즘으로 안되고 다이나믹 프로그래밍(DP)으로 풀어야 함.
"""

n = 1260
cnt = 0

all_coin = [500, 100, 50, 10]

for coin in all_coin:
    cnt += n // coin
    n %= coin

print(cnt)

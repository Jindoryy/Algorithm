temp = []
for _ in range(9):
    temp.append(int(input()))
temp.sort()

temp2 = sum(temp) - 100

for i in range(9-1):
    for j in range(i+1, 9):
        sum = temp[i] + temp[j]

        if sum == temp2:
            break
    if sum == temp2:
        break

a, b = temp[i], temp[j]
temp.remove(a)
temp.remove(b)

for i in temp:
    print(i)



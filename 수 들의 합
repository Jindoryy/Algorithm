n, m = map(int, input().split())
num = list(map(int, input().split()))
cnt = 0
lt, rt = 0, 1
sum = num[0]

while True:
    if sum < m:
        if rt < n:
            sum += num[rt]
            rt += 1
        else:
            break
    if sum == m:
        cnt += 1
        sum -= num[lt]
        lt += 1
    if sum > m:
        sum -= num[lt]
        lt += 1

print(cnt)

# 투 포인터를 이용한건데 굉장히 헷갈리고 어려움
# 다시 봐야할 필요성이 있음
# 특히 sum -= num[lt], if rt < n: 이부분

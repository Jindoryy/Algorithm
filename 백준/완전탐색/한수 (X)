import sys
input = sys.stdin.readline

n = int(input())
cnt = 0

for i in range(1, n+1):
    if 1 <= i < 100:
        cnt += 1
    else:
        tmp = list(map(int, str(i)))
        if tmp[0] - tmp[1] == tmp[1] - tmp[2]:
            cnt += 1

print(cnt)

import sys
input = sys.stdin.readline

n = int(input())
meet = []
cnt = 0

for _ in range(n):
    a, b = map(int, input().split())
    meet.append((a, b))

meet.sort(key = lambda x : (x[1], x[0]))
end = 0

for s, e in meet:
    if end <= s:
        end = e
        cnt += 1

print(cnt)

n = int(input())
tmp = []

for _ in range(n):
    a, b = map(int, input().split())
    tmp.append((a, b))

tmp.sort(key=lambda x : (x[1], x[0]))

s, cnt = 0, 1

for i in range(1, n):
    if tmp[s][1] <= tmp[i][0]:
        s = i
        cnt += 1

print(cnt)

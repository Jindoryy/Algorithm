def Subset(v):
    if v == n+1:
        for i in range(1, n+1):
            if ch[i] == 1:
                print(i, end=' ')
        print()
    else:
        ch[v] = 1
        Subset(v+1)
        ch[v] = 0
        Subset(v + 1)

if __name__ == "__main__":
    n = int(input())
    ch = [0] * (n+1) # 인덱스 1부터 이용
    Subset(1)
    
# 함수에서 else: 부분 주목

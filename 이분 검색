n, m = map(int, input().split())
a = list(map(int, input().split()))

a.sort()
lt, rt = 0, n-1

while lt <= rt:
    mid = (lt + rt) // 2

    if m == a[mid]:
        print(mid+1)
        break
    elif m < a[mid]:
        rt = mid - 1
    else:
        lt = mid + 1
        
# 간단한 이분 탐색 문제
